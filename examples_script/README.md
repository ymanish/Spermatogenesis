Active validation and test-style example scripts.

Use this directory for scripts that are primarily meant to verify that active
modules run correctly, or to provide small-scale exploratory validation runs.

Keep this directory focused on:
- local validation scripts
- active test/example entry points
- lightweight reproducibility checks for current workflows

Do not keep deprecated, superseded, or unused examples here. Those should move
to `archive/examples_script/`.
