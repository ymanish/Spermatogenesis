Archived core modules that are no longer part of the active simulation stack.

These files were moved out of `src/core` to reduce navigation overhead in the
main codebase. They are kept for historical reference only.

Current archived topics here include:
- tau-leaping
- hybrid rejection

If one of these approaches is revived later, restore it intentionally rather
than importing from this archive location.
