Archived scripts moved out of `src/scripts`.

These files are kept for historical reference or one-off reproducibility, but
they are not part of the active workflow and should not be treated as
maintained entry points.

Current policy:
- Keep actively used operational scripts in `src/scripts/`.
- Move obsolete, superseded, or one-off scripts here.
- Before archiving a script, ensure nothing still imports it from active code.
